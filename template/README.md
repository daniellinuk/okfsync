# Bagsy template

Minimal OKF knowledge bundle for the bagsy collide-then-recover demo.

```bash
# from monorepo root (after building the CLI)
bun run demo
bun run lint
```

Concepts live in `concepts/`. Locks live in `.bagsy/locks/`.
